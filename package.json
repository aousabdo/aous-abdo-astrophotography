{
  "name": "aous-abdo-astrophotography",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && rm -rf dist && vite build --base=/ --outDir dist/client && node scripts/generate-static-routes.mjs && mkdir -p dist/server && cp worker/index.js dist/server/index.js",
    "build:pages": "tsc -b && rm -rf dist && vite build --outDir dist/client && node scripts/generate-static-routes.mjs",
    "store:check": "node --check store-worker/src/index.js && node --test store-worker/test/*.test.js && wrangler deploy --dry-run --config store-worker/wrangler.jsonc",
    "store:dev": "wrangler dev --config store-worker/wrangler.jsonc",
    "store:deploy": "wrangler deploy --config store-worker/wrangler.jsonc",
    "store:migrate:local": "wrangler d1 migrations apply one-more-photon-orders --local --config store-worker/wrangler.jsonc",
    "store:migrate:remote": "wrangler d1 migrations apply one-more-photon-orders --remote --config store-worker/wrangler.jsonc",
    "preview": "vite preview --outDir dist/client",
    "lint": "eslint ."
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest"
  },
  "devDependencies": {
    "wrangler": "4.113.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
